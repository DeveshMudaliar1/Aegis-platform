Aegis: The AI Assurance Platform (Audit Compliant)## Quick Start1. Backend: cd backend && pip install -r requirements.txt2. Frontend: cd frontend && npm install3. Run: docker-compose up --build4. Audit: Run ./test_aegis.sh to verify security headers.5. Access: http://localhost:3000## Audit Fixes Implemented- Runtime Injection: env.sh injects variables at container startup.- Strict CORS: Regex whitelist for dynamic Cloud Run URLs.- Defensive Math: ZeroDivisionError protection in VIX calculation.- Stable Visualization: React Flow infinite loop prevention (nodes decoupled).- Cost Opt: Added .dockerignore to prevent large uploads.